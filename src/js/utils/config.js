import { SELECTORS, CLASSES } from './constants';

export const defaultFormConfig = {
  formSelector: SELECTORS.POPUP_FORM,
  inputSelector: SELECTORS.POPUP_INPUT,
  submitButtonSelector: SELECTORS.POPUP_BUTTON,
  inactiveButtonClass: CLASSES.POPUP_BUTTON_DISABLED,
  inputErrorClass: CLASSES.POPUP_INPUT_ERROR,
  errorClass: CLASSES.POPUP_ERROR,
};

// отличное решение
